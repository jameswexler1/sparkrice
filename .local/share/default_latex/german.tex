% ============================================================
%  Deutsch täglich — Language Study Template
%  Pandoc LaTeX template  →  XeLaTeX → PDF
%  Place at: ~/.local/share/default_latex/german.tex
% ============================================================

\documentclass[11pt, a4paper]{article}

% ── Page geometry ────────────────────────────────────────────
\usepackage[
  a4paper,
  left=2cm, right=2cm,
  top=2.4cm, bottom=2.8cm,
  headheight=15pt
]{geometry}

% ── Font & encoding (XeLaTeX) ────────────────────────────────
\usepackage{fontspec}
\usepackage{microtype}
\setmainfont{Libertinus Serif}
\setsansfont{Libertinus Sans}
\setmonofont[Scale=0.88]{DejaVu Sans Mono}  % swap for JetBrains Mono if installed

% ── Language (German + English, XeLaTeX-native) ──────────────
\usepackage{polyglossia}
\setdefaultlanguage{english}
\setotherlanguage[spelling=new, babelshorthands=false]{german}

% ── Colors ───────────────────────────────────────────────────
\usepackage{xcolor}
\definecolor{deblue}{RGB}{15, 55, 120}       % primary accent — deep blue for contrast
\definecolor{degreen}{RGB}{30, 110, 65}      % correct / vocab
\definecolor{dered}{RGB}{160, 30, 30}        % warning / error
\definecolor{deamber}{RGB}{180, 110, 0}      % exercise / tip
\definecolor{depurple}{RGB}{90, 40, 130}     % dialog
\definecolor{degray}{RGB}{100, 100, 100}     % subtle text
\definecolor{rulegray}{RGB}{190, 190, 190}   % rule lines
\definecolor{bggray}{RGB}{248, 248, 246}     % light bg for boxes
\definecolor{bgblue}{RGB}{240, 245, 252}
\definecolor{bggreen}{RGB}{240, 250, 244}
\definecolor{bgamber}{RGB}{252, 247, 235}
\definecolor{bgpurple}{RGB}{246, 241, 252}
\definecolor{bgred}{RGB}{252, 241, 241}

% ── Tables ───────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{array}
\usepackage{multirow}
\usepackage{float}
\usepackage{needspace}
% suppress "Table N:" captions from Pandoc longtables
\usepackage{caption}
\DeclareCaptionFormat{blank}{}
\captionsetup{format=blank, aboveskip=0pt, belowskip=0pt}

% ── Lists ────────────────────────────────────────────────────
\usepackage{enumitem}
\setlist{noitemsep, topsep=3pt, parsep=1pt, leftmargin=1.4em}
\setlist[itemize,1]{label=\textcolor{deblue}{\textbullet}}
\setlist[itemize,2]{label=\textcolor{degray}{\textendash}}
\setlist[enumerate,1]{label=\textcolor{deblue}{\arabic*.}}

% ── Graphics ─────────────────────────────────────────────────
\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth, keepaspectratio}

% ── tcolorbox ────────────────────────────────────────────────
\usepackage[most]{tcolorbox}

% ── Custom boxes ─────────────────────────────────────────────

% Grammar explanation box
\newtcolorbox{grammarbox}[1][]{
  colback=bgblue, colframe=deblue,
  title={\small\bfseries\color{white}\ifx&#1&Grammatik\else#1\fi},
  coltitle=white, fonttitle=\small\bfseries,
  arc=3pt, boxrule=0pt, leftrule=3pt,
  left=6pt, right=6pt, top=5pt, bottom=5pt,
  before upper={\setlength{\parskip}{4pt}}
}

% Exercise box
\newtcolorbox{uebungbox}{
  colback=bgamber, colframe=deamber,
  title={\small\bfseries\color{white} Übung},
  coltitle=white, fonttitle=\small\bfseries,
  arc=3pt, boxrule=0pt, leftrule=3pt,
  left=6pt, right=6pt, top=5pt, bottom=5pt,
  before upper={\setlength{\parskip}{4pt}}
}

% Answer box (collapsible feel — just styled differently)
\newtcolorbox{antwortbox}{
  colback=bggreen, colframe=degreen,
  title={\small\bfseries\color{white} Antworten},
  coltitle=white, fonttitle=\small\bfseries,
  arc=3pt, boxrule=0pt, leftrule=3pt,
  left=6pt, right=6pt, top=5pt, bottom=5pt,
  before upper={\setlength{\parskip}{3pt}}
}

% Reading text box
\newtcolorbox{lesetextbox}{
  colback=bggray, colframe=rulegray,
  arc=3pt, boxrule=0.5pt,
  left=8pt, right=8pt, top=6pt, bottom=6pt,
  before upper={\setlength{\parskip}{5pt}},
  breakable
}

% Dialog box
\newtcolorbox{dialogbox}{
  colback=bgpurple, colframe=depurple,
  arc=3pt, boxrule=0pt, leftrule=3pt,
  left=8pt, right=8pt, top=6pt, bottom=6pt,
  before upper={\setlength{\parskip}{4pt}}
}

% Writing prompt box
\newtcolorbox{schreibbox}{
  colback=bgred, colframe=dered,
  title={\small\bfseries\color{white} Schreibaufgabe},
  coltitle=white, fonttitle=\small\bfseries,
  arc=3pt, boxrule=0pt, leftrule=3pt,
  left=6pt, right=6pt, top=5pt, bottom=5pt,
  before upper={\setlength{\parskip}{4pt}}
}

% Tip / Merke box
\newtcolorbox{merkebox}{
  colback=bgamber, colframe=deamber,
  arc=3pt, boxrule=0pt, leftrule=3pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
  fontupper=\small
}

% ── Custom commands ───────────────────────────────────────────

% German word highlight (bold + deep blue — visible on white and gray backgrounds)
\newcommand{\de}[1]{\textbf{\textcolor{deblue}{#1}}}

% English translation (italic gray)
\newcommand{\en}[1]{\textit{\textcolor{degray}{#1}}}

% Grammar term
\newcommand{\gram}[1]{\textsc{\textcolor{deblue}{#1}}}

% Correct answer marker
\newcommand{\richtig}[1]{\textcolor{degreen}{\textbf{#1}}}

% Wrong / blank marker for exercises
\newcommand{\blank}{\underline{\hspace{2.5cm}}}

% Section label pill (small colored tag)
\newcommand{\phasetag}[1]{%
  \hfill{\small\sffamily\bfseries\textcolor{white}%
  \colorbox{deblue}{\strut\ #1\ }}%
}

% Day number display
\newcommand{\daynumber}[1]{%
  {\large\sffamily\bfseries\textcolor{deblue}{Tag #1}}%
}

% ── Section formatting ────────────────────────────────────────
\usepackage{titlesec}

\titleformat{\section}{
  \normalfont\large\bfseries\sffamily\color{deblue}
}{}{0em}{}[{\color{rulegray}\vspace{-6pt}\rule{\linewidth}{0.6pt}}]

\titleformat{\subsection}{
  \normalfont\normalsize\bfseries\color{deblue}
}{}{0em}{}

\titleformat{\subsubsection}{
  \normalfont\normalsize\itshape\color{degray}
}{}{0em}{}

\titlespacing{\section}{0pt}{14pt}{5pt}
\titlespacing{\subsection}{0pt}{10pt}{3pt}
\titlespacing{\subsubsection}{0pt}{7pt}{2pt}

% ── Paragraph spacing ────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{5pt}

% ── Header / Footer ──────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.4pt}
\fancyhead[L]{%
  \small\sffamily\textcolor{degray}{%
    \textbf{Deutsch B1{\,→\,}C1}\enspace·\enspace
    $if(subject)$$subject$$else$Deutsch täglich$endif$%
  }%
}
\fancyhead[R]{%
  \small\sffamily\textcolor{degray}{%
    $if(title)$$title$$endif$%
  }%
}
\fancyfoot[L]{\small\sffamily\textcolor{degray}{$if(date)$$date$$endif$}}
\fancyfoot[C]{\small\sffamily\textcolor{degray}{\thepage}}
\fancyfoot[R]{\small\sffamily\textcolor{degray}{$if(phase)$$phase$$endif$}}

% ── Hyperlinks ───────────────────────────────────────────────
\usepackage{hyperref}
\hypersetup{
  colorlinks = true,
  linkcolor  = deblue,
  urlcolor   = deblue,
  citecolor  = dered,
  pdftitle   = {$if(title)$$title$$endif$},
  pdfauthor  = {Deutsch täglich}
}

% ── Quotes ───────────────────────────────────────────────────
\usepackage{csquotes}

% ── Pandoc boilerplate ───────────────────────────────────────
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% ── Metadata ─────────────────────────────────────────────────
\author{}
$if(title)$
  \title{%
    \vspace{-1.5em}%
    \sffamily\bfseries\Large\textcolor{deblue}{$title$}\\[4pt]
    \normalfont\normalsize\textcolor{degray}{$if(subtitle)$$subtitle$$endif$}%
  }
$else$
  \title{}
$endif$
$if(date)$\date{\small\sffamily\textcolor{degray}{$date$}}$else$\date{}$endif$

% ============================================================
\begin{document}

\maketitle
\vspace{-1em}
{\color{rulegray}\rule{\linewidth}{0.8pt}}
\vspace{0.5em}
\thispagestyle{fancy}

$if(toc)$
\tableofcontents
\clearpage
$endif$

$body$

\end{document}
