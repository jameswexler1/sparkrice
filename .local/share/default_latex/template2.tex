% Custom Pandoc LaTeX Template based on Gustavo Magalhães's .tex file
\documentclass{article}
\usepackage[paper=a4paper, left=2cm, top=3cm, right=2cm, bottom=3cm]{geometry}
\usepackage{amsmath}
\usepackage{titlesec}
\usepackage{longtable}
\usepackage{xcolor}
\usepackage{csquotes}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{wrapfig}
\usepackage{float}
\usepackage[american]{babel}
% Set column separation rules
\renewcommand{\columnseprule}{0.4pt}
\setlength{\columnsep}{20pt}
% Hyperref (moved after xcolor, before biblatex)
\usepackage{hyperref}
\hypersetup{
  colorlinks = true,
  linkcolor  = black,
  urlcolor   = blue,
  citecolor  = red
}
% Bibliography (only when --biblatex is passed)
$if(biblatex)$
\usepackage[backend=biber, style=apa]{biblatex}
\addbibresource{/home/gustavo/.local/share/biblatex/uni.bib}
$endif$
$if(author)$
  \author{$author$}
$else$
  \author{}
$endif$
$if(title)$
  \title{$title$}
$else$
  \title{}
$endif$
$if(date)$
  \date{$date$}
$else$
  \date{\today}
$endif$
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\begin{document}
\maketitle
$if(toc)$
\tableofcontents
\clearpage
$endif$
$body$
$if(biblatex)$
\newpage
\printbibliography
$endif$
\end{document}
